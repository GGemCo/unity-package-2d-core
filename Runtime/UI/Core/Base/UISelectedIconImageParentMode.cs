namespace GGemCo2DCore
{
    /// <summary>
    /// 선택 아이콘 이미지 오브젝트를 어느 부모 아래에 배치할지 정의합니다.
    /// </summary>
    public enum UISelectedIconImageParentMode
    {
        /// <summary>
        /// 메인 UI 캔버스 아래에 선택 아이콘 이미지를 배치합니다.
        /// </summary>
        MainCanvas,

        /// <summary>
        /// 선택된 아이콘 오브젝트 아래에 선택 아이콘 이미지를 배치합니다.
        /// </summary>
        SelectedIcon
    }
}
